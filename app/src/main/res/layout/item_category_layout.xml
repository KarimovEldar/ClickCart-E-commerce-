<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@color/backgroundColor"
    android:layout_margin="8dp">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/itemCategoryImageView"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:padding="4dp"
        android:scaleType="centerCrop"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearanceOverlay="@style/Circular"
        app:strokeColor="@color/light_blue"
        app:strokeWidth="1dp"
        tools:srcCompat="@drawable/test_category" />

    <TextView
        android:id="@+id/itemCategoryTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/shoe"
        android:fontFamily="@font/poppins"
        android:textColor="@color/light_purple"
        android:textSize="12dp"
        app:layout_constraintEnd_toEndOf="@+id/itemCategoryImageView"
        app:layout_constraintStart_toStartOf="@+id/itemCategoryImageView"
        app:layout_constraintTop_toBottomOf="@+id/itemCategoryImageView"/>


</androidx.constraintlayout.widget.ConstraintLayout>